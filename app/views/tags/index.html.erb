<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
      <li>
        <%= link_to tag.name, tag, class: 'tag_title', id: "tag_#{tag.id}" %>
        <% if logged_in? %>
            <%= link_to '[delete]', tag, method: :delete %>
        <% end %>
      </li>
  <% end %>
</ul>